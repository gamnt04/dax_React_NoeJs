
import logo from "@/assets/logo.png"
const Logo = () => {
    return (
        <div>
            <img height={130} width={130} alt="logo" src={logo} />
        </div>
    );
};

export default Logo;
